"""Optional Headroom extensions.

Modules in this package are not imported automatically; they are
loaded only when explicitly referenced via ``--bedrock-client-hook`` (or
similar opt-in entry points). See ``bedrock_refresh.py`` for the
canonical refreshable-session example.
"""
