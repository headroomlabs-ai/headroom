[package]
name = "headroom-cli"
version = "0.1.0"
edition = "2021"
authors = ["Peter Lodri <peter@vaked.dev>"]
description = "Headroom CLI — Offline Neural Weight Sparsity & SVD Energy Decay Calculator"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
